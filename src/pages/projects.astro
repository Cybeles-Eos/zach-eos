---
import { getCollection } from "astro:content";
import Layout from "../layouts/Layout.astro";
import Navbar from "../partials/navbar.astro";
import Header from "../partials/header.astro";

const fullstackProjects = await getCollection("fullstackProjects");
const completedProjectCount = fullstackProjects.length;
const projectRows = fullstackProjects
	.filter((project) => project.data.isActive !== false)
	.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
	.map((project) => {
		const projectType = project.data.category.toLowerCase().includes("figma")
			? "figma-project"
			: "web-project";

		return {
			title: project.data.title,
			description: project.data.description,
			year: project.data.year,
			category: project.data.category,
			projectType,
			searchText: [
				project.data.title,
				project.data.description,
				project.data.year,
				project.data.category,
			].join(" "),
			link: `/projects/${project.id}`,
			thumbnail: project.data.thumbnail,
			thumbnailAlt: project.data.thumbnailAlt,
			previewMedia: project.data.previewMedia,
		};
	});

---

<Layout
	title="Projects | Zach Portfolio"
	description="Browse Zach's projects — fullstack development and UI/UX design work across websites, portfolios, and web systems."
>
	<main class="fullstack">
		<Header />
		<Navbar />

		<section class="section--hero glb-sec-pad-out">
			<div class="section--hero__grid" aria-hidden="true"></div>
			<div class="section--hero__con">
				<div class="section--hero__main glb-sec-pad">
					 
					<!-- <h1>Explore the work that helped me build my <span>skills</span></h1> -->
                    <h1>See how my ideas turned into finished <span>web designs</span>.</h1>
					<p>Explore a collection of design projects I’ve worked on through freelance work, school and personal builds.</p>
					<img src="/images/uiux-vector.svg" alt="vector" class="vector-svg-uix">
					<svg width="2158" height="2158" class="section--hero--svg" viewBox="0 0 2158 2158" fill="none" xmlns="http://www.w3.org/2000/svg">
						<g opacity="0.2" filter="url(#filter0_f_2690_2519)">
						<circle cx="1078.62" cy="1078.62" r="523.621" fill="#00A14C"/>
						</g>
						<g opacity="0.2" filter="url(#filter1_f_2690_2519)">
						<circle cx="1115.2" cy="1071.25" r="412.48" fill="#07D468"/>
						</g>
						<g opacity="0.2" filter="url(#filter2_f_2690_2519)">
						<circle cx="1115.2" cy="1049.99" r="290.366" fill="white"/>
						</g>
						<defs>
						<filter id="filter0_f_2690_2519" x="0" y="0" width="2157.24" height="2157.24" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						<filter id="filter1_f_2690_2519" x="147.716" y="103.766" width="1934.96" height="1934.96" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						<filter id="filter2_f_2690_2519" x="269.831" y="204.621" width="1690.73" height="1690.73" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						</defs>
					</svg>		
				</div>
			</div>
		</section>

		<section class="section--main glb-sec-pad-out">
			<div class="section--main__con">
				<div class="section--main__main">

                    <div class="nav-prj">
                        <div class="nav-prj__top"> 
                            <div class="prj-search-bar"><div><input type="text" placeholder="Search projects by type..." data-project-search=""><svg width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M15.7491 15.7491L12.4941 12.4941" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M8.25 14.25C11.5637 14.25 14.25 11.5637 14.25 8.25C14.25 4.93629 11.5637 2.25 8.25 2.25C4.93629 2.25 2.25 4.93629 2.25 8.25C2.25 11.5637 4.93629 14.25 8.25 14.25Z" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"></path></svg></div></div>
                            <div class="prj-filter" data-project-type-filter data-project-type-value="all-projects">
                                <button class="prj-filter__toggle" type="button" aria-haspopup="listbox" aria-expanded="false" data-project-type-toggle>
                                    <span data-project-type-label>All</span>
                                    <svg class="prj-filter__chevron" width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
                                        <path d="M4.5 6.75L9 11.25L13.5 6.75" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
                                    </svg>
                                </button>
                                <div class="prj-filter__menu" role="listbox" data-project-type-menu>
                                    <button class="prj-filter__option is-active" type="button" role="option" aria-selected="true" data-project-type-option value="all-projects">All</button>
                                    <button class="prj-filter__option" type="button" role="option" aria-selected="false" data-project-type-option value="web-project">Web Project</button>
                                    <button class="prj-filter__option" type="button" role="option" aria-selected="false" data-project-type-option value="figma-project">Figma Project</button>
                                </div>
                            </div>
                        </div>
                        <div class="legends-prj">
                            <div class="legends-prj__box">
                                <div class="crl crl__green"></div>
                                <p>Web Projects</p>
                            </div>
                            <!-- <div class="legends-prj__box">
                                <div class="crl crl__blue"></div>
                                <p>Figma</p>
                            </div> -->
                            <div class="legends-prj__box">
                                <div class="crl crl__purple"></div>
                                <p>Figma</p>
                            </div>
                        </div>
                    </div>
					
                    
                    <div class="project-list"  data-project-preview-list>

                        {projectRows.map((project) => (
                            <a
                                href={project.link}
                                class="project-list-box"
                                data-project-preview-row
                                data-project-filter-row
                                data-project-type={project.projectType}
                                data-project-search-text={project.searchText}
                                data-preview-type={project.previewMedia.type}
                                data-preview-src={project.previewMedia.src}
                                data-preview-alt={project.previewMedia.alt}
                                data-preview-poster={project.previewMedia.poster ?? ""}
                            >
                                <div class="project-list-box__left">
                                    <div class="dots">
                                        <span></span>
                                        <span></span>
                                    </div>
                                    <div class="details">
                                        <h3>{project.title} </h3>
                                        <p>{project.description}</p>
                                    </div>
                                </div>
                                <div class="project-list-box__right">
                                    <img src={project.thumbnail} alt={project.thumbnailAlt}>
                                    <svg width="23" height="22" viewBox="0 0 23 22" fill="none" xmlns="http://www.w3.org/2000/svg">
                                    <path d="M16.8401 12.1457L-0.00179153 12.1457L-0.00179141 9.37843L16.8401 9.37843L9.41814 1.95649L11.3746 -4.70426e-07L22.1367 10.7621L11.3746 21.5242L9.41814 19.5677L16.8401 12.1457Z" fill="currentColor"/>
                                    </svg>
                                </div>
                            </a>
                        ))}

						<div class="project-list-empty" data-project-empty-state aria-hidden="true">
							<p>No matching projects found.</p>
						</div>
					
						<div class="project-hover-preview" data-project-hover-preview aria-hidden="true"></div>
					</div>

				</div>
				
				<svg width="10" height="16" class="left-trb-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M4.99219 0H0.0302734H0V15.0172H4.99609V10.0172H10.0039V5.01671H4.99219V0Z" fill="currentColor"/>
				</svg>
				<svg width="10" height="16" class="right-tlb-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M5.01172 15.0172L9.97363 15.0172L10.0039 15.0172L10.0039 0L5.00781 4.36772e-07L5.00781 5L-8.75732e-07 5L-4.38575e-07 10.0005L5.01172 10.0005L5.01172 15.0172Z" fill="currentColor"/>
				</svg>
			</div>
		</section>

		<section class="section--cta glb-sec-pad-out">
			<div class="section--cta__con">
				<div class="section--cta__main glb-sec-pad">

					<h2>Lets turn <span>ideas</span> into digital experiences</h2>
					<p class="section--cta__main--p">Whether it's a website, application, or digital solution, I can help turn your ideas into a functional and modern experience.</p>
					<a href="/projects" class="cls-cta">
						<span class="cls-cta__icon">
							<svg width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
								<path d="M9 16.5C13.1421 16.5 16.5 13.1421 16.5 9C16.5 4.85786 13.1421 1.5 9 1.5C4.85786 1.5 1.5 4.85786 1.5 9C1.5 13.1421 4.85786 16.5 9 16.5Z" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
								<path d="M16.5 9H13.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
								<path d="M4.5 9H1.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
								<path d="M9 4.5V1.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
								<path d="M9 16.5V13.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
							</svg>
						</span>
						<span class="cls-cta__text">
							<span class="cls-cta__title">Lets start a project</span>
							<span class="cls-cta__count">{completedProjectCount} projects complete</span>
						</span>
					</a>
					<svg width="2158" height="2158" class="section--cta__main--svg" viewBox="0 0 2158 2158" fill="none" xmlns="http://www.w3.org/2000/svg">
						<g opacity="0.2" filter="url(#filter0_f_2690_2519)">
						<circle cx="1078.62" cy="1078.62" r="523.621" fill="#00A14C"/>
						</g>
						<g opacity="0.2" filter="url(#filter1_f_2690_2519)">
						<circle cx="1115.2" cy="1071.25" r="412.48" fill="#07D468"/>
						</g>
						<g opacity="0.2" filter="url(#filter2_f_2690_2519)">
						<circle cx="1115.2" cy="1049.99" r="290.366" fill="white"/>
						</g>
						<defs>
						<filter id="filter0_f_2690_2519" x="0" y="0" width="2157.24" height="2157.24" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						<filter id="filter1_f_2690_2519" x="147.716" y="103.766" width="1934.96" height="1934.96" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						<filter id="filter2_f_2690_2519" x="269.831" y="204.621" width="1690.73" height="1690.73" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB">
						<feFlood flood-opacity="0" result="BackgroundImageFix"/>
						<feBlend mode="normal" in="SourceGraphic" in2="BackgroundImageFix" result="shape"/>
						<feGaussianBlur stdDeviation="277.5" result="effect1_foregroundBlur_2690_2519"/>
						</filter>
						</defs>
					</svg>
					
					
				</div>
				
				<svg width="10" height="16" class="left-trb-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M4.99219 0H0.0302734H0V15.0172H4.99609V10.0172H10.0039V5.01671H4.99219V0Z" fill="currentColor"/>
				</svg>
				<svg width="10" height="16" class="right-tlb-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M5.01172 15.0172L9.97363 15.0172L10.0039 15.0172L10.0039 0L5.00781 4.36772e-07L5.00781 5L-8.75732e-07 5L-4.38575e-07 10.0005L5.01172 10.0005L5.01172 15.0172Z" fill="currentColor"/>
				</svg>
				<svg width="10" height="16" class="left-trb-n-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M4.99219 0H0.0302734H0V15.0172H4.99609V10.0172H10.0039V5.01671H4.99219V0Z" fill="currentColor"/>
				</svg>
				<svg width="10" height="16" class="right-tlb-n-vec" viewBox="0 0 10 16" fill="none" xmlns="http://www.w3.org/2000/svg">
					<path d="M5.01172 15.0172L9.97363 15.0172L10.0039 15.0172L10.0039 0L5.00781 4.36772e-07L5.00781 5L-8.75732e-07 5L-4.38575e-07 10.0005L5.01172 10.0005L5.01172 15.0172Z" fill="currentColor"/>
				</svg>
			</div>
		</section>

		<section class="section--cls glb-sec-pad-out">
			<div class="section--cls__con">
				<div class="section--cls__main">

				</div>
			</div>
		</section>
	</main>
</Layout>
<script>
	import EmblaCarousel from "embla-carousel";

	let featuredProjectsEmbla;

	const initFeaturedProjects = () => {
		const featuredProjects = document.querySelector(".featured-projects");

		if (!featuredProjects) return;

		if (featuredProjectsEmbla) {
			featuredProjectsEmbla.destroy();
			featuredProjectsEmbla = undefined;
		}

		featuredProjectsEmbla = EmblaCarousel(featuredProjects, {
			loop: false,
			dragFree: true,
			containScroll: "trimSnaps"
		});
	};

	const initProjectFilters = () => {
		const searchInput = document.querySelector("[data-project-search]");
		const typeFilter = document.querySelector("[data-project-type-filter]");
		const filterToggle = document.querySelector("[data-project-type-toggle]");
		const filterLabel = document.querySelector("[data-project-type-label]");
		const filterOptions = [...document.querySelectorAll("[data-project-type-option]")];
		const projectRows = [...document.querySelectorAll("[data-project-filter-row]")];
		const emptyState = document.querySelector("[data-project-empty-state]");
		const preview = document.querySelector("[data-project-hover-preview]");

		if (!searchInput || !typeFilter || !filterToggle || !filterLabel || !projectRows.length) return;

		const hidePreview = () => {
			preview?.classList.remove("is-visible");
			preview?.setAttribute("aria-hidden", "true");
		};

		const normalize = (value) => value.toLowerCase().trim();

		const applyFilters = () => {
			const searchValue = normalize(searchInput.value);
			const selectedType = typeFilter.dataset.projectTypeValue || "all-projects";
			let visibleCount = 0;

			projectRows.forEach((row) => {
				const rowType = row.dataset.projectType || "";
				const rowSearchText = normalize(row.dataset.projectSearchText || "");
				const matchesType = selectedType === "all-projects" || rowType === selectedType;
				const matchesSearch = !searchValue || rowSearchText.includes(searchValue);
				const shouldShow = matchesType && matchesSearch;

				row.classList.toggle("is-filtered-out", !shouldShow);
				if (shouldShow) visibleCount += 1;
			});

			emptyState?.classList.toggle("is-visible", visibleCount === 0);
			emptyState?.setAttribute("aria-hidden", visibleCount === 0 ? "false" : "true");
			hidePreview();
		};

		const openSelect = () => {
			typeFilter.classList.add("is-open");
			filterToggle.setAttribute("aria-expanded", "true");
		};

		const closeSelect = () => {
			typeFilter.classList.remove("is-open");
			filterToggle.setAttribute("aria-expanded", "false");
		};

		const toggleSelect = () => {
			if (typeFilter.classList.contains("is-open")) {
				closeSelect();
				return;
			}

			openSelect();
		};

		const setSelectedType = (option) => {
			const nextValue = option.value || "all-projects";
			typeFilter.dataset.projectTypeValue = nextValue;
			filterLabel.textContent = option.textContent;

			filterOptions.forEach((item) => {
				const isActive = item === option;
				item.classList.toggle("is-active", isActive);
				item.setAttribute("aria-selected", isActive ? "true" : "false");
			});

			applyFilters();
			closeSelect();
		};

		searchInput.addEventListener("input", applyFilters);
		filterToggle.addEventListener("click", toggleSelect);
		filterOptions.forEach((option) => {
			option.addEventListener("click", () => setSelectedType(option));
		});
		document.addEventListener("click", (event) => {
			if (!typeFilter.contains(event.target)) closeSelect();
		});
		document.addEventListener("keydown", (event) => {
			if (event.key === "Escape") closeSelect();
		});

		applyFilters();
	};

	let teardownProjectHoverPreview = () => {};

	const initProjectHoverPreview = () => {
		teardownProjectHoverPreview();

		const projectList = document.querySelector("main.fullstack [data-project-preview-list]");
		const preview = document.querySelector("[data-project-hover-preview]");

		if (!projectList || !preview) return;

		const padding = 20;
		const offset = 22;
		let currentSrc = "";
		let currentX = padding;
		let currentY = padding;
		let targetX = padding;
		let targetY = padding;
		let animationFrame;
		let isPreviewVisible = false;
		let previewWidth = 330;
		let previewHeight = 206;
		const previewRows = [...projectList.querySelectorAll("[data-project-preview-row]")];
		const mediaCache = new Map();
		const cleanups = [];

		const applyPreviewPosition = () => {
			preview.style.setProperty("--preview-x", `${currentX}px`);
			preview.style.setProperty("--preview-y", `${currentY}px`);
		};

		const animatePreviewPosition = () => {
			const easing = 0.18;
			const deltaX = targetX - currentX;
			const deltaY = targetY - currentY;

			currentX += deltaX * easing;
			currentY += deltaY * easing;

			if (Math.abs(deltaX) < 0.4) currentX = targetX;
			if (Math.abs(deltaY) < 0.4) currentY = targetY;

			applyPreviewPosition();

			if (isPreviewVisible && (currentX !== targetX || currentY !== targetY)) {
				animationFrame = requestAnimationFrame(animatePreviewPosition);
				return;
			}

			animationFrame = undefined;
		};

		const queuePreviewPosition = () => {
			if (animationFrame) return;
			animationFrame = requestAnimationFrame(animatePreviewPosition);
		};

		const updatePreviewSize = () => {
			const rect = preview.getBoundingClientRect();
			previewWidth = rect.width;
			previewHeight = rect.height;
		};

		const getPreviewPosition = (event) => {
			const viewportWidth = window.innerWidth;
			const viewportHeight = window.innerHeight;
			let nextX = event.clientX + offset;
			let nextY = event.clientY + offset;

			nextX = Math.min(Math.max(nextX, padding), viewportWidth - previewWidth - padding);
			nextY = Math.min(Math.max(nextY, padding), viewportHeight - previewHeight - padding);

			return { x: nextX, y: nextY };
		};

		const setPreviewPosition = (event, snap = false) => {
			const nextPosition = getPreviewPosition(event);
			targetX = nextPosition.x;
			targetY = nextPosition.y;

			if (snap) {
				currentX = targetX;
				currentY = targetY;
				applyPreviewPosition();
				return;
			}

			queuePreviewPosition();
		};

		const resetPreviewPan = () => {
			preview.classList.remove("is-panning");
			preview.style.setProperty("--preview-scroll-distance", "0px");
		};

		const setPreviewPan = (mediaWidth, mediaHeight) => {
			if (!mediaWidth || !mediaHeight) return;

			const renderedMediaHeight = previewWidth * (mediaHeight / mediaWidth);
			const scrollDistance = Math.max(0, renderedMediaHeight - previewHeight);

			if (scrollDistance <= 8) {
				resetPreviewPan();
				return;
			}

			preview.style.setProperty("--preview-scroll-distance", `-${scrollDistance}px`);
			preview.classList.add("is-panning");
		};

		const syncPreviewPan = (media) => {
			if (media instanceof HTMLVideoElement) {
				setPreviewPan(media.videoWidth, media.videoHeight);
				return;
			}

			setPreviewPan(media.naturalWidth, media.naturalHeight);
		};

		const getPreviewMediaData = (row) => ({
			type: row.dataset.previewType,
			src: row.dataset.previewSrc,
			alt: row.dataset.previewAlt || "",
			poster: row.dataset.previewPoster || ""
		});

		const createPreviewMedia = ({ type, src, alt, poster }) => {
			if (type === "video") {
				const video = document.createElement("video");
				video.muted = true;
				video.loop = true;
				video.playsInline = true;
				video.preload = "metadata";
				video.autoplay = true;
				video.setAttribute("aria-label", alt);
				if (poster) video.poster = poster;
				video.addEventListener("loadedmetadata", () => {
					if (currentSrc === src && preview.contains(video)) syncPreviewPan(video);
				});
				video.src = src;
				return video;
			}

			const image = new Image();
			image.alt = alt;
			image.decoding = "async";
			image.addEventListener("load", () => {
				if (currentSrc === src && preview.contains(image)) syncPreviewPan(image);
			});
			image.src = src;
			image.decode?.().catch(() => {});
			return image;
		};

		const getCachedMedia = (row) => {
			const mediaData = getPreviewMediaData(row);
			if (!mediaData.src) return undefined;

			if (!mediaCache.has(mediaData.src)) {
				mediaCache.set(mediaData.src, createPreviewMedia(mediaData));
			}

			return mediaCache.get(mediaData.src);
		};

		const preloadPreviewMedia = () => {
			previewRows.forEach((row) => getCachedMedia(row));
		};

		const setPreviewMedia = (row) => {
			const { src } = getPreviewMediaData(row);

			if (!src || src === currentSrc) return;

			const media = getCachedMedia(row);
			if (!media) return;

			currentSrc = src;
			resetPreviewPan();
			preview.replaceChildren(media);
			updatePreviewSize();

			if (media instanceof HTMLVideoElement) {
				if (media.readyState >= 1) syncPreviewPan(media);
				media.play().catch(() => {});
			} else if (media.complete) {
				syncPreviewPan(media);
			} else {
				resetPreviewPan();
			}
		};

		const showPreview = (event) => {
			const row = event.currentTarget;
			setPreviewMedia(row);
			updatePreviewSize();
			setPreviewPosition(event, !isPreviewVisible);
			isPreviewVisible = true;
			preview.classList.add("is-visible");
			preview.setAttribute("aria-hidden", "false");
			queuePreviewPosition();
		};

		const hidePreview = () => {
			isPreviewVisible = false;
			if (animationFrame) {
				cancelAnimationFrame(animationFrame);
				animationFrame = undefined;
			}
			preview.classList.remove("is-visible");
			preview.setAttribute("aria-hidden", "true");
		};

		const hidePreviewOnRowLeave = (event) => {
			const nextTarget = event.relatedTarget;
			if (nextTarget instanceof Element && nextTarget.closest("[data-project-preview-row]")) return;
			hidePreview();
		};

		preloadPreviewMedia();

		previewRows.forEach((row) => {
			row.addEventListener("pointerenter", showPreview);
			row.addEventListener("pointermove", setPreviewPosition);
			row.addEventListener("pointerleave", hidePreviewOnRowLeave);
			cleanups.push(() => {
				row.removeEventListener("pointerenter", showPreview);
				row.removeEventListener("pointermove", setPreviewPosition);
				row.removeEventListener("pointerleave", hidePreviewOnRowLeave);
			});
		});

		projectList.addEventListener("pointerleave", hidePreview);
		window.addEventListener("blur", hidePreview);
		window.addEventListener("resize", updatePreviewSize);

		cleanups.push(() => {
			projectList.removeEventListener("pointerleave", hidePreview);
			window.removeEventListener("blur", hidePreview);
			window.removeEventListener("resize", updatePreviewSize);
			hidePreview();
		});

		teardownProjectHoverPreview = () => {
			cleanups.forEach((cleanup) => cleanup());
			teardownProjectHoverPreview = () => {};
		};
	};

	document.addEventListener("astro:page-load", initFeaturedProjects);
	document.addEventListener("astro:page-load", initProjectFilters);
	document.addEventListener("astro:page-load", initProjectHoverPreview);
	document.addEventListener("astro:before-swap", () => teardownProjectHoverPreview());
</script>
